package com.example.project1

import android.content.Context
import android.content.DialogInterface
import android.content.Intent
import android.view.View

/*
public class ImageClickListener(context : Context, position : Int) : DialogInterface.OnClickListener {
    var mcontext = context
    var mposition = position

    public fun onClick(v : View){
        // 확대된 이미지를 보여주는 액티비티를 실행하기 위해 인텐트 객체를 정의합니다.
        // 그리고 이 액티비티에 전달할 imageID의 값을 이 객체에 저장합니다.
        // 인텐트 객체를 정의 후 이 액티비티를 실행합니다.
        val intent : Intent = Intent(mcontext, ImageActivity)
        intent.putExtra("image ID", mposition)
        mcontext.startActivity(intent)
    }
}*/